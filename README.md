# 台股關注熱度搜尋器

本機小工具：股票名稱 / 代號搜尋，結合 Google Trends 與 PTT Stock 討論熱度。

詳見 [`stock-heat-tracker/README.md`](stock-heat-tracker/README.md)。

```bash
cd stock-heat-tracker
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
streamlit run app.py
```
